{
  "name": "dnemis-knowledge-base",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf _site && eleventy && pagefind --site _site --output-subdir pagefind",
    "start": "eleventy --serve --port 8080",
    "test": "npm run build && node --test tests/*.test.js"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "cheerio": "^1.0.0",
    "js-yaml": "^4.1.0",
    "pagefind": "^1.1.0",
    "pdf-lib": "^1.17.1",
    "playwright": "^1.62.0"
  }
}
